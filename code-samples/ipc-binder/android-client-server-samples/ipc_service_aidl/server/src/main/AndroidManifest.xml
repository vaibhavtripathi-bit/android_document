<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar">

        <activity
            android:name=".ServerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".BookManagerService"
            android:exported="true"
            android:permission="com.example.aidlserver.BIND_BOOK_SERVICE">
            <intent-filter>
                <action android:name="com.example.aidlserver.IBookManager" />
            </intent-filter>
        </service>

        <!-- Custom permission to protect the service -->
        <permission
            android:name="com.example.aidlserver.BIND_BOOK_SERVICE"
            android:protectionLevel="normal"
            android:label="Bind to Book Manager Service" />

    </application>
</manifest>
