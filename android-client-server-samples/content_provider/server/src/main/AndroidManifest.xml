<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.example.cpserver.READ_NOTES"
        android:protectionLevel="normal" />
    <permission
        android:name="com.example.cpserver.WRITE_NOTES"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar">

        <activity
            android:name=".ServerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".NoteProvider"
            android:authorities="com.example.cpserver.notes"
            android:exported="true"
            android:readPermission="com.example.cpserver.READ_NOTES"
            android:writePermission="com.example.cpserver.WRITE_NOTES" />

    </application>
</manifest>
